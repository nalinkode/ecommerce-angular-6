export interface Product {
  productId: number;
  productName: string;
  productPrice : number,
  productOfferPrice : number,
  productDescription : string,
  productColor : string,
  size: string,
  isStock : boolean,
  quantity : number
 }
